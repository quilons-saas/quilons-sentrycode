sentrycode:
  image: node:20
  stage: test
  script:
    - npm ci
    - npm run build
    - node dist/cli/main.js check . --ci --format json --output .sentrycode/out/sentrycode.json
  artifacts:
    when: always
    paths:
      - .sentrycode/out/
